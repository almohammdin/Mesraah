import { GoogleGenAI, Modality } from 'https://cdn.jsdelivr.net/npm/@google/genai@2.14.0/+esm';
import { TASK_TOOL_DECLARATIONS, executeTaskTool } from './mesraah-voice-tools.js?v=0.18.0';
const MODEL='gemini-3.1-flash-live-preview',INPUT_RATE=16000,OUTPUT_RATE=24000,DATA_KEY='mesraah_v030',HISTORY_KEY='mesraah_voice_history_v1',TOOL_TIMEOUT_MS=12000;
let active=false,session=null,micStream=null,micContext=null,outputContext=null,micSource=null,micProcessor=null,silentGain=null,outputWorklet=null,outputGain=null,micSuppressed=false,outputQueuedUntil=0,resumeMicTimer=null,streamEndSent=false;
function isIOS(){return /iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1)}const IOS_ECHO_GUARD=isIOS();function endpoint(){return String(window.MESRAAH_VOICE_TOKEN_ENDPOINT||'').trim()}function readState(){try{return JSON.parse(localStorage.getItem(DATA_KEY)||'{}')||{}}catch{return {}}}function history(){try{return JSON.parse(localStorage.getItem(HISTORY_KEY)||'[]').slice(-12)}catch{return []}}function remember(role,text){text=String(text||'').trim();if(!text)return;const h=history();if(h.at(-1)?.role===role&&h.at(-1)?.text===text)return;h.push({role,text,at:Date.now()});localStorage.setItem(HISTORY_KEY,JSON.stringify(h.slice(-12)))}
function contextInstruction(){const state=readState(),tasks=(state.tasks||[]).filter(t=>t.status!=='done').sort((a,b)=>(a.due||'9999').localeCompare(b.due||'9999')).slice(0,40).map(t=>({id:t.id,title:t.title,notes:t.notes||'',due:t.due||'',time:t.time||'',follow:t.follow||'',status:t.status||'',priority:t.priority||'',spaceId:t.spaceId||'',personId:t.personId||''})),people=(state.people||[]).slice(0,40).map(p=>({id:p.id,name:p.name})),spaces=(state.spaces||[]).slice(0,30).map(s=>({id:s.id,name:s.name})),calendar=window.MesraahCalendar?.getCachedEvents?.().slice(0,20)||[],now=new Intl.DateTimeFormat('ar-SA-u-ca-gregory-nu-latn',{timeZone:'Asia/Riyadh',dateStyle:'full',timeStyle:'medium'}).format(new Date());return `أنت مسراح، مساعد شخصي سعودي صوتي بصوت رجل. تكلم بطبيعية واختصار وبلهجة سعودية سهلة.
الوقت الحالي في الرياض: ${now}.
مسراح نفسه هو مصدر الحقيقة. عند أي سؤال عن مهمة موجودة استخدم search_tasks حتى لو كانت مذكورة في سياق المحادثة. عند الإضافة استخدم add_task. عند التعديل ابحث أولا ثم update_task. عند الحذف ابحث ثم delete_task. عند الإنجاز استخدم complete_task.
ممنوع أن تقول تم أو أضفت أو عدلت أو حذفت أو أنجزت إلا إذا رجعت الأداة ok=true بعد التحقق والحفظ. إذا فشل الحفظ قل إن التنفيذ لم يكتمل.
بعد فتح جلسة جديدة لا تعتمد على ذاكرتك فقط؛ اقرأ بيانات مسراح الحالية بالأداة عند الحاجة.
ردودك قصيرة ومناسبة للمحادثة الصوتية.
بيانات مسراح الحالية:${JSON.stringify({name:state.profile?.name||'',tasks,people,spaces,calendar})}
آخر سياق محادثة محفوظ:${JSON.stringify(history())}`}
function injectStyles(){if(document.getElementById('mesraahVoiceStyles'))return;const s=document.createElement('style');s.id='mesraahVoiceStyles';s.textContent='.mesraah-voice-detail{min-height:50px;margin:10px auto 15px;padding:10px 12px;border:1px solid rgba(255,255,255,.11);border-radius:14px;background:rgba(255,255,255,.055);color:rgba(255,255,255,.82);font-size:11px;line-height:1.75;text-align:right;overflow-wrap:anywhere}.mesraah-voice-actions{display:flex;justify-content:center;gap:8px;flex-wrap:wrap}.v80-voice-overlay[data-state="connecting"] .v80-voice-orb{animation:v80Breath 1.25s ease-in-out infinite}.v80-voice-overlay[data-state="listening"] .v80-voice-orb{animation:v80Breath 1s ease-in-out infinite}.v80-voice-overlay[data-state="speaking"] .v80-voice-orb{animation:v80Breath .7s ease-in-out infinite}';document.head.appendChild(s)}
function ensureUi(){document.getElementById('v80VoiceOverlay')?.remove();const h=document.createElement('div');h.id='v80VoiceOverlay';h.className='v80-voice-overlay';h.hidden=true;h.innerHTML='<section class="v80-voice-card" role="dialog" aria-modal="true" aria-label="محادثة صوتية مع مسراح"><div class="v80-voice-top"><div><span class="v80-voice-kicker">مسراح</span><strong>تحدث مع مسراح</strong></div><button type="button" id="mesraahVoiceClose" aria-label="إغلاق">×</button></div><div class="v80-voice-orb"><span></span><span></span><span></span></div><div class="v80-voice-status" id="mesraahVoiceStatus">جاهز</div><div class="mesraah-voice-detail" id="mesraahVoiceDetail">تكلم بشكل طبيعي، ومسراح يسمعك ويتعامل مع مهامك.</div><div class="mesraah-voice-actions"><button type="button" class="v80-voice-stop" id="mesraahVoiceStop">إنهاء المحادثة</button></div></section>';document.body.appendChild(h);document.getElementById('mesraahVoiceClose').onclick=stop;document.getElementById('mesraahVoiceStop').onclick=stop}function setStatus(t,state=''){const e=document.getElementById('mesraahVoiceStatus');if(e)e.textContent=t;const h=document.getElementById('v80VoiceOverlay');if(h)h.dataset.state=state}function setDetail(t){const e=document.getElementById('mesraahVoiceDetail');if(e)e.textContent=String(t||'')}
function bytesToBase64(bytes){let b='';for(let i=0;i<bytes.length;i+=0x8000)b+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(b)}function base64ToFloat32(v){const b=atob(v),n=b.length-(b.length%2),buf=new ArrayBuffer(n),bytes=new Uint8Array(buf);for(let i=0;i<n;i++)bytes[i]=b.charCodeAt(i);const pcm=new Int16Array(buf),out=new Float32Array(pcm.length);for(let i=0;i<pcm.length;i++)out[i]=pcm[i]/32768;return out}function resampleToInt16(input,rate){const ratio=rate/INPUT_RATE,out=new Int16Array(Math.max(1,Math.round(input.length/ratio)));for(let i=0;i<out.length;i++){const pos=i*ratio,l=Math.floor(pos),r=Math.min(l+1,input.length-1),mix=pos-l,v=(input[l]||0)*(1-mix)+(input[r]||0)*mix,c=Math.max(-1,Math.min(1,v));out[i]=c<0?c*32768:c*32767}return out}
async function prepareAudio(){const A=window.AudioContext||window.webkitAudioContext;if(!A||!navigator.mediaDevices?.getUserMedia)throw new Error('voice-not-supported');micContext=new A();try{outputContext=new A({sampleRate:OUTPUT_RATE})}catch{outputContext=new A()}await Promise.all([micContext.resume(),outputContext.resume()]);await outputContext.audioWorklet.addModule('./mesraah-voice-playback.worklet.js?v=0.12.4');outputWorklet=new AudioWorkletNode(outputContext,'mesraah-voice-playback');outputGain=outputContext.createGain();outputWorklet.connect(outputGain);outputGain.connect(outputContext.destination);outputQueuedUntil=outputContext.currentTime;micStream=await navigator.mediaDevices.getUserMedia({audio:{channelCount:1,echoCancellation:true,noiseSuppression:true,autoGainControl:true}})}function clearPlayback(){try{outputWorklet?.port.postMessage({type:'clear'})}catch{}if(outputContext)outputQueuedUntil=outputContext.currentTime}function suppressMicForModelOutput(){if(!IOS_ECHO_GUARD||micSuppressed)return;micSuppressed=true;if(!streamEndSent&&session){streamEndSent=true;try{session.sendRealtimeInput({audioStreamEnd:true})}catch{}}}function resumeMicAfterPlayback(){clearTimeout(resumeMicTimer);if(!IOS_ECHO_GUARD){if(active)setStatus('أسمعك الآن','listening');return}const ms=outputContext?Math.max(0,(outputQueuedUntil-outputContext.currentTime)*1000):0;resumeMicTimer=setTimeout(()=>{micSuppressed=false;streamEndSent=false;if(active)setStatus('أسمعك الآن','listening')},ms+140)}function playPcm(b64){if(!active||!outputContext||!outputWorklet||!b64)return;suppressMicForModelOutput();const samples=base64ToFloat32(b64);if(!samples.length)return;outputQueuedUntil=Math.max(outputContext.currentTime,outputQueuedUntil)+samples.length/OUTPUT_RATE;try{outputWorklet.port.postMessage({samples},[samples.buffer])}catch{outputWorklet.port.postMessage({samples})}setStatus('مسراح يتكلم','speaking')}
async function fetchToken(refresh=false){const url=endpoint();if(!url||typeof window.MesraahVoiceGetAppCheckToken!=='function')throw new Error('voice-connection-not-ready');const token=await window.MesraahVoiceGetAppCheckToken({forceRefresh:refresh}),r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json','X-Firebase-AppCheck':token},body:'{}'}),d=await r.json().catch(()=>({}));if(r.status===401&&!refresh)return fetchToken(true);if(!r.ok||!d.token)throw new Error('voice-token-failed');return d.token}function withTimeout(p,ms=TOOL_TIMEOUT_MS){let t;return Promise.race([Promise.resolve(p),new Promise((_,rej)=>t=setTimeout(()=>rej(new Error('voice-tool-timeout')),ms))]).finally(()=>clearTimeout(t))}
async function persistAndVerify(call,result){if(!result?.ok||call.name==='search_tasks')return result;await new Promise(r=>setTimeout(r,60));const state=readState();if(['add_task','update_task','complete_task'].includes(call.name)){const taskId=result.task?.id||call.args?.taskId,task=(state.tasks||[]).find(t=>String(t.id)===String(taskId));if(!task)return {ok:false,error:'task-persistence-not-confirmed'};if(call.name==='complete_task'&&task.status!=='done')return {ok:false,error:'task-completion-not-confirmed'}}if(call.name==='delete_task'&&(state.tasks||[]).some(t=>String(t.id)===String(call.args?.taskId)))return {ok:false,error:'task-delete-not-confirmed'};try{const cloud=await withTimeout(window.MesraahCloudBridge?.saveNow?.()||Promise.resolve({ok:true,mode:'local'}),8000);if(cloud?.ok===false)return {ok:false,error:'cloud-save-not-confirmed'}}catch{return {ok:false,error:'cloud-save-failed'}}window.dispatchEvent(new CustomEvent('mesraah:data-changed',{detail:{type:'assistant-persisted',taskId:result.task?.id||call.args?.taskId||''}}));return {...result,persisted:true}}
async function handleToolCalls(calls=[]){if(!session||!calls.length)return;setStatus('أنفذ طلبك…','connecting');const responses=[];for(const call of calls){let result;try{result=await withTimeout(executeTaskTool(call.name,call.args||{}));result=await persistAndVerify(call,result)}catch(e){result={ok:false,error:String(e?.message||e)}}responses.push({name:call.name,id:call.id,response:{result}})}try{session.sendToolResponse({functionResponses:responses})}catch(e){console.error('Mesraah voice tool response:',e);setDetail('تعذر إكمال التنفيذ الآن.')}}function handleMessage(m){if(m?.toolCall?.functionCalls?.length)handleToolCalls(m.toolCall.functionCalls).catch(console.error);const c=m?.serverContent;if(!c)return;if(c.interrupted){clearPlayback();micSuppressed=false;streamEndSent=false;setStatus('أسمعك الآن','listening')}if(c.inputTranscription?.text&&!micSuppressed){remember('user',c.inputTranscription.text);setDetail(`أنت: ${c.inputTranscription.text}`)}if(c.outputTranscription?.text){remember('assistant',c.outputTranscription.text);setDetail(`مسراح: ${c.outputTranscription.text}`)}for(const p of c.modelTurn?.parts||[])if(p.inlineData?.data)playPcm(p.inlineData.data);if(c.turnComplete&&active)resumeMicAfterPlayback()}
function startMic(){if(!active||!session||!micContext||!micStream||micProcessor)return;micSource=micContext.createMediaStreamSource(micStream);micProcessor=micContext.createScriptProcessor(2048,1,1);silentGain=micContext.createGain();silentGain.gain.value=0;micProcessor.onaudioprocess=e=>{if(!active||!session||micSuppressed)return;const pcm=resampleToInt16(e.inputBuffer.getChannelData(0),micContext.sampleRate),bytes=new Uint8Array(pcm.buffer,pcm.byteOffset,pcm.byteLength);try{session.sendRealtimeInput({audio:{data:bytesToBase64(bytes),mimeType:`audio/pcm;rate=${INPUT_RATE}`}})}catch{}};micSource.connect(micProcessor);micProcessor.connect(silentGain);silentGain.connect(micContext.destination)}function stopMic(){if(micProcessor)micProcessor.onaudioprocess=null;try{micProcessor?.disconnect();micSource?.disconnect();silentGain?.disconnect()}catch{}micProcessor=micSource=silentGain=null}async function shutdownMedia(){clearTimeout(resumeMicTimer);stopMic();clearPlayback();try{outputWorklet?.disconnect();outputGain?.disconnect()}catch{}outputWorklet=outputGain=null;micStream?.getTracks?.().forEach(t=>t.stop());micStream=null;try{await micContext?.close();await outputContext?.close()}catch{}micContext=outputContext=null}
async function start(){if(active)return;ensureUi();injectStyles();document.getElementById('v80VoiceOverlay').hidden=false;active=true;setStatus('أجهز المحادثة…','connecting');try{await prepareAudio();const token=await fetchToken(),ai=new GoogleGenAI({apiKey:token,httpOptions:{apiVersion:'v1alpha'}});session=await ai.live.connect({model:MODEL,config:{responseModalities:[Modality.AUDIO],systemInstruction:contextInstruction(),inputAudioTranscription:{},outputAudioTranscription:{},speechConfig:{voiceConfig:{prebuiltVoiceConfig:{voiceName:'Orus'}}},tools:[{functionDeclarations:TASK_TOOL_DECLARATIONS}]},callbacks:{onmessage:handleMessage,onerror:e=>console.error('Mesraah voice error:',e),onclose:()=>{if(active){setStatus('انقطع الاتصال','error');setDetail('انقطع الاتصال الصوتي. حاول مرة أخرى.')}}});if(!active)return;setStatus('أسمعك الآن','listening');setDetail('تكلم بشكل طبيعي. أقدر أبحث في مهامك وأضيفها وأعدلها وأحذفها وأنجزها بأمرك.');startMic()}catch(e){console.error('Mesraah voice start:',e);setStatus('تعذر تشغيل المحادثة الصوتية','error');setDetail('تعذر الاتصال الآن. حاول مرة أخرى بعد قليل.');await shutdownMedia();active=false}}async function stop(){active=false;try{session?.close?.()}catch{}session=null;await shutdownMedia();const h=document.getElementById('v80VoiceOverlay');if(h)setTimeout(()=>h.hidden=true,150)}window.MesraahVoice={start,stop,get active(){return active}};injectStyles();ensureUi();