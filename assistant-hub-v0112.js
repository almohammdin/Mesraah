const HISTORY_KEY='mesraah_assistant_history_v1';
const DATA_KEY='mesraah_v030';
let chatBusy=false;
const NAV_ACTIONS=new Set(['open_today','open_calendar','open_inbox','open_spaces','open_people','open_followups','open_achievements','open_rewards','open_space','open_person','open_task']);
function escapeHtml(value=''){return String(value).replace(/[&<>\"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[char]))}function readState(){try{return JSON.parse(localStorage.getItem(DATA_KEY)||'{}')||{}}catch{return {}}}function recordHistory(userText,result){try{const current=JSON.parse(sessionStorage.getItem(HISTORY_KEY)||'[]'),history=Array.isArray(current)?current:[];history.push({role:'user',text:userText});history.push({role:'assistant',text:result?.reply||'',action:result?.action||null});sessionStorage.setItem(HISTORY_KEY,JSON.stringify(history.slice(-8)))}catch{}}function timeout(ms=19000){return new Promise((_,reject)=>setTimeout(()=>{const error=new Error('assistant-timeout');error.code='assistant-timeout';reject(error)},ms))}function setTaskField(id,value){const el=document.getElementById(id);if(el)el.value=value||''}function nameById(list,id){return(list||[]).find(item=>String(item.id)===String(id))?.name||''}
async function prepareTaskAction(action,{submit=false}={}){setTaskField('taskId','');setTaskField('taskTitle',action.title||'');setTaskField('taskNotes',action.notes||'');setTaskField('taskSpace',action.spaceId||'');setTaskField('taskPerson',action.personId||'');setTaskField('taskStatus','inbox');setTaskField('taskPriority',action.priority||'normal');setTaskField('taskDue',action.date||'');setTaskField('taskFollow','');setTaskField('taskPoints','10');const title=document.getElementById('taskModalTitle');if(title)title.textContent='مهمة جديدة';const del=document.getElementById('deleteTaskBtn');if(del)del.hidden=true;const dialog=document.getElementById('taskModal');if(dialog&&!dialog.open)dialog.showModal();await new Promise(resolve=>setTimeout(resolve,35));const due=document.getElementById('v11DueGregorian');if(due){due.value=action.date||'';due.dispatchEvent(new Event('change',{bubbles:true}))}const time=document.getElementById('v11TaskTime');if(time)time.value=action.time||'';if(submit)document.getElementById('taskForm')?.requestSubmit()}
async function addTaskAction(action){const state=readState();return window.MesraahTaskTools?.execute?.('add_task',{title:action.title,due:action.date||'',time:action.time||'',location:action.location||'',notes:action.notes||'',priority:action.priority||'normal',status:'inbox',spaceName:nameById(state.spaces,action.spaceId),peopleNames:nameById(state.people,action.personId)?[nameById(state.people,action.personId)]:[],repeat:action.repeat||'none',repeatInterval:action.repeatInterval||1,repeatCount:action.repeatCount||0,repeatUntil:action.repeatUntil||''})}
function clickView(view){const button=document.querySelector(`.nav-item[data-view="${view}"]`)||document.querySelector(`[data-open-view="${view}"]`);button?.click();return Boolean(button)}
function openEntity(kind,id){const selector=`[data-open-entity="${kind}"][data-entity-id="${CSS.escape(String(id||''))}"]`;const button=document.querySelector(selector);button?.click();return Boolean(button)}
function openTask(id){const button=document.querySelector(`[data-edit="${CSS.escape(String(id||''))}"]`);button?.click();return Boolean(button)}
async function executeAction(action){if(!action||action.type==='none')return{ok:false};const viewMap={open_today:'today',open_calendar:'calendar',open_inbox:'inbox',open_spaces:'spaces',open_people:'people',open_followups:'followups',open_achievements:'achievements',open_rewards:'rewards'};if(viewMap[action.type])return{ok:clickView(viewMap[action.type]),kind:'navigation'};if(action.type==='open_space')return{ok:openEntity('space',action.spaceId),kind:'navigation'};if(action.type==='open_person')return{ok:openEntity('person',action.personId),kind:'navigation'};if(action.type==='open_task')return{ok:openTask(action.taskId),kind:'navigation'};if(action.type==='connect_calendar'){await window.MesraahCalendar?.connect?.();return{ok:Boolean(window.MesraahCalendar?.status?.().connected),kind:'connect'}}if(action.type==='task'){const result=await addTaskAction(action);return{ok:Boolean(result?.ok),kind:'task',task:result?.task,error:result?.error}}if(action.type==='calendar'){const result=await addTaskAction(action);if(!result?.ok)return{ok:false,error:result?.error||'task-save-failed'};return{ok:true,kind:'task',task:result.task}}return{ok:false}}
function appendMessage(role,html){const transcript=document.getElementById('v112ChatTranscript');if(!transcript)return null;const item=document.createElement('div');item.className=`v112-chat-message ${role}`;item.innerHTML=html;transcript.appendChild(item);transcript.scrollTop=transcript.scrollHeight;return item}function thinkingMessage(){return appendMessage('assistant','<span class="v112-thinking"><i></i><i></i><i></i></span>')}
function actionLabel(action){if(action?.label)return action.label;const labels={open_today:'فتح اليوم',open_calendar:'فتح التقويم',open_inbox:'فتح الوارد',open_spaces:'فتح المساحات',open_people:'فتح الأشخاص',open_followups:'فتح المتابعات',open_achievements:'فتح الإنجاز',open_rewards:'فتح المكافآت',open_space:'فتح المساحة',open_person:'فتح الشخص',open_task:'فتح المهمة'};if(labels[action?.type])return labels[action.type];if(action?.type==='calendar')return'أضف الموعد';if(action?.type==='connect_calendar')return'اربط التقويم';return'أضف المهمة'}
function renderAssistantResult(result){const action=result?.action||{type:'none'},hasAction=action.type!=='none'&&(NAV_ACTIONS.has(action.type)||action.title||action.type==='connect_calendar'),meta=[action.date,action.time,action.location].filter(Boolean).map(escapeHtml).join(' • ');const message=appendMessage('assistant',`<div class="v112-chat-answer">${escapeHtml(result?.reply||'تفضل.').replace(/\n/g,'<br>')}</div>${hasAction?`<div class="v112-chat-action"><div><strong>${escapeHtml(action.title||actionLabel(action))}</strong>${meta?`<small>${meta}</small>`:''}</div><div class="v112-chat-action-buttons"><button type="button" data-v112-do>${escapeHtml(actionLabel(action))}</button>${action.type==='task'?'<button type="button" class="secondary" data-v112-edit>تعديل قبل الحفظ</button>':''}</div></div>`:''}`);message?.querySelector('[data-v112-do]')?.addEventListener('click',async event=>{const button=event.currentTarget;button.disabled=true;const done=await executeAction(action);button.textContent=done.ok?(done.kind==='navigation'?'تم الفتح':'تم التنفيذ'):'تعذر التنفيذ';if(!done.ok)button.disabled=false});message?.querySelector('[data-v112-edit]')?.addEventListener('click',()=>void prepareTaskAction(action,{submit:false}))}
async function runChat(){const input=document.getElementById('v112ChatInput'),send=document.getElementById('v112ChatSend');if(!input||!send||chatBusy)return;const text=input.value.trim();if(!text)return;const hasAttachment=Array.isArray(window.MesraahPendingAttachments)&&window.MesraahPendingAttachments.length>0;chatBusy=true;send.disabled=true;input.disabled=true;appendMessage('user',`<div class="v112-chat-answer">${escapeHtml(text)}</div>`);input.value='';const thinking=thinkingMessage();try{const ask=window.MesraahAssistant?.ask;if(typeof ask!=='function')throw new Error('assistant-not-ready');const result=await Promise.race([ask(text),timeout(hasAttachment?52000:19000)]);thinking?.remove();if(!result)throw new Error('assistant-empty');if(NAV_ACTIONS.has(result.action?.type)){const done=await executeAction(result.action);if(done.ok){result.reply=result.reply||'تفضل.';result.action={type:'none'}}}else if(result.confirmed&&['task','calendar','connect_calendar'].includes(result.action?.type)){const done=await executeAction(result.action);if(done.ok){result.reply=`${result.reply||''} وتم التنفيذ.`.trim();result.action={type:'none'}}}renderAssistantResult(result);recordHistory(text,result)}catch(error){console.error('Mesraah text assistant:',error);thinking?.remove();appendMessage('assistant',`<div class="v112-chat-answer">${hasAttachment?'تعذر قراءة المرفق الآن. المرفق ما زال موجودا؛ جرب الإرسال مرة ثانية.':'تعذر الرد الآن. جرب مرة ثانية.'}</div>`)}finally{chatBusy=false;send.disabled=false;input.disabled=false;input.focus()}}
function installHubStyles(){}
function installHub(){installHubStyles();document.querySelector('.fly-card')?.classList.add('v112-hidden-fly');const card=document.getElementById('v11VoiceCard');if(!card||card.dataset.v112Hub)return;card.dataset.v112Hub='1';const copy=card.querySelector('.v11-voice-copy'),action=card.querySelector('.v11-voice-action');if(copy){copy.querySelector('.v11-voice-kicker')?.replaceChildren(document.createTextNode('مسراح معك'));const h2=copy.querySelector('h2');if(h2)h2.textContent='ماذا تريد أن تنجز اليوم؟';const p=copy.querySelector('p');if(p)p.textContent='اكتب أو تحدث بشكل طبيعي، ومسراح يوجهك وينفذ معك داخل المنصة.';copy.querySelector('.v11-voice-examples')?.remove()}if(action)action.innerHTML='<div class="v112-hub-orb" aria-hidden="true"><span>✦</span></div><div class="v112-hub-buttons"><button type="button" class="v112-hub-primary" id="v112VoiceStart"><span>🎙</span><strong>محادثة صوتية</strong></button><button type="button" class="v112-hub-secondary" id="v112TextStart"><span>⌨</span><strong>محادثة كتابية</strong></button></div>';const chat=document.createElement('div');chat.id='v112TextChat';chat.className='v112-text-chat';chat.hidden=true;chat.innerHTML='<div class="v112-chat-head"><strong>محادثة كتابية مع مسراح</strong><div><button type="button" class="v112-chat-close" id="v112CloseChat">إغلاق المحادثة</button><button type="button" id="v112NewChat">محادثة جديدة</button></div></div><div class="v112-chat-transcript" id="v112ChatTranscript" aria-live="polite"><div class="v112-chat-message assistant"><div class="v112-chat-answer">اكتب لي بشكل طبيعي. أقدر أرتب لك يومك، وأفتح المساحات والأشخاص والمهام، وأنفذ معك داخل مسراح.</div></div></div><div class="v112-chat-compose"><input id="v112ChatInput" maxlength="300" autocomplete="off" placeholder="اكتب لمسراح…" aria-label="اكتب رسالتك لمسراح"><button type="button" id="v112ChatSend">إرسال</button></div>';card.appendChild(chat);document.getElementById('v112VoiceStart')?.addEventListener('click',()=>window.MesraahVoice?.start?.());document.getElementById('v112TextStart')?.addEventListener('click',()=>{chat.hidden=false;setTimeout(()=>document.getElementById('v112ChatInput')?.focus(),50)});document.getElementById('v112CloseChat')?.addEventListener('click',()=>{chat.hidden=true});document.getElementById('v112ChatSend')?.addEventListener('click',runChat);document.getElementById('v112ChatInput')?.addEventListener('keydown',event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();void runChat()}});document.getElementById('v112NewChat')?.addEventListener('click',()=>{window.MesraahAssistant?.clearHistory?.();sessionStorage.removeItem(HISTORY_KEY);const transcript=document.getElementById('v112ChatTranscript');if(transcript)transcript.innerHTML='<div class="v112-chat-message assistant"><div class="v112-chat-answer">بدأنا من جديد. وش ودك تسوي؟</div></div>'})}
function boot(){installHub();setTimeout(installHub,300)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
