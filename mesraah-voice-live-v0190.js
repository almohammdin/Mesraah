import {GoogleGenAI,Modality} from 'https://cdn.jsdelivr.net/npm/@google/genai@2.14.0/+esm';
import {TASK_TOOL_DECLARATIONS,executeTaskTool} from './mesraah-voice-tools.js?v=0.19.0';

const MODEL='gemini-3.1-flash-live-preview',INPUT_RATE=16000,OUTPUT_RATE=24000,TOOL_TIMEOUT_MS=15000,DATA_KEY='mesraah_v030';
let active=false,session=null,micStream=null,micContext=null,outputContext=null,micSource=null,micProcessor=null,silentGain=null,outputWorklet=null,outputGain=null,micSuppressed=false,outputQueuedUntil=0,resumeMicTimer=null,streamEndSent=false;
function isIOS(){return /iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1)}
const IOS_ECHO_GUARD=isIOS();
const endpoint=()=>String(window.MESRAAH_VOICE_TOKEN_ENDPOINT||'https://mesraah-live-token.naif123456.workers.dev/token').trim();
function readState(){try{return JSON.parse(localStorage.getItem(DATA_KEY)||'{}')||{}}catch{return {}}}
function currentContext(){const state=readState();const calendar=window.MesraahCalendar?.getCachedEvents?.()||[];return{profile:state.profile||{},tasks:(state.tasks||[]).map(t=>({id:t.id,title:t.title||'',notes:t.notes||'',due:t.due||'',time:t.time||'',location:t.location||null,follow:t.follow||'',status:t.status||'',priority:t.priority||'',impact:t.impact??null,ease:t.ease??null,spaceId:t.spaceId||'',personId:t.personId||'',calendarEventId:t.calendarEventId||''})),spaces:(state.spaces||[]).map(s=>({id:s.id,name:s.name||''})),people:(state.people||[]).map(p=>({id:p.id,name:p.name||'',relation:p.relation||'',city:p.city||'',organization:p.organization||''})),calendar};}
function contextInstruction(){const context=currentContext();return `أنت مسراح، مساعد شخصي سعودي ذكي داخل منصة مسراح. هذه محادثة صوتية حقيقية عبر Gemini وليست قراءة آلية للنصوص.
تكلم بعربية سعودية سهلة وطبيعية وباختصار. افهم مقصد المستخدم، ولا تقرأ الصفحة أو القوائم حرفيا.
مسراح نفسه هو مصدر الحقيقة والذاكرة العملية. بيانات المهام والمساحات والأشخاص والتقويم المرفقة هي حالة مسراح الحالية عند بدء الجلسة.
عند أي سؤال عن مهمة موجودة استخدم search_tasks أولا للحصول على الحالة الأحدث. عند الإضافة استخدم add_task، وعند التعديل ابحث ثم update_task، وعند الحذف ابحث ثم delete_task، وعند الإنجاز استخدم complete_task.
إذا طلب المستخدم عدة مهام صراحة فتعامل معها كلها ولا تتوقف بعد الأولى. إذا كان قصده غير محسوم اسأله سؤالا واحدا قصيرا.
لا تقل تم أو أضفت أو عدلت أو حذفت أو أنجزت إلا بعد رجوع الأداة ok=true، وبعد أي تغيير يجب تأكيد الحفظ. إذا تعذر الحفظ قل ذلك مباشرة.
استفد من الأثر والسهولة والأولوية والمواعيد عندما يسأل المستخدم بماذا يبدأ.
لا تخترع بيانات غير موجودة في مسراح.
حالة مسراح الحالية:
${JSON.stringify(context)}`}
function ensureUi(){if(document.getElementById('v80VoiceOverlay'))return;const host=document.createElement('div');host.id='v80VoiceOverlay';host.className='v80-voice-overlay';host.hidden=true;host.innerHTML='<section class="v80-voice-card" role="region" aria-label="محادثة صوتية مع مسراح"><div class="v80-voice-top"><div><span class="v80-voice-kicker">مسراح</span><strong>محادثة صوتية ذكية</strong></div><button type="button" id="mesraahVoiceClose" aria-label="إغلاق">×</button></div><div class="v80-voice-orb"><span></span><span></span><span></span></div><div class="v80-voice-status" id="mesraahVoiceStatus" aria-live="polite">جاهز</div><div class="mesraah-voice-detail" id="mesraahVoiceDetail">تكلم بشكل طبيعي. مسراح يسمعك ويفهمك وينفذ معك.</div><div class="mesraah-voice-actions"><button type="button" class="v80-voice-stop" id="mesraahVoiceStop">إنهاء المحادثة</button></div></section>';document.body.appendChild(host);document.getElementById('mesraahVoiceClose').onclick=stop;document.getElementById('mesraahVoiceStop').onclick=stop}
function injectStyles(){if(document.getElementById('mesraahLiveVoice0190Styles'))return;const style=document.createElement('style');style.id='mesraahLiveVoice0190Styles';style.textContent='.mesraah-voice-detail{min-height:50px;margin:10px auto 15px;padding:10px 12px;border:1px solid rgba(255,255,255,.11);border-radius:14px;background:rgba(255,255,255,.055);color:rgba(255,255,255,.82);font-size:11px;line-height:1.75;text-align:right;overflow-wrap:anywhere}.mesraah-voice-actions{display:flex;justify-content:center;gap:8px;flex-wrap:wrap}.v80-voice-overlay[data-state="connecting"] .v80-voice-orb{animation:v80Breath 1.25s ease-in-out infinite}.v80-voice-overlay[data-state="listening"] .v80-voice-orb{animation:v80Breath 1s ease-in-out infinite}.v80-voice-overlay[data-state="speaking"] .v80-voice-orb{animation:v80Breath .7s ease-in-out infinite}';document.head.appendChild(style)}
function setStatus(text,state=''){const el=document.getElementById('mesraahVoiceStatus');if(el)el.textContent=text;const host=document.getElementById('v80VoiceOverlay');if(host)host.dataset.state=state}
function setDetail(text){const el=document.getElementById('mesraahVoiceDetail');if(el)el.textContent=String(text||'')}
function bytesToBase64(bytes){let binary='';for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(binary)}
function base64ToFloat32(value){const binary=atob(value),length=binary.length-(binary.length%2),buffer=new ArrayBuffer(length),bytes=new Uint8Array(buffer);for(let i=0;i<length;i++)bytes[i]=binary.charCodeAt(i);const pcm=new Int16Array(buffer),out=new Float32Array(pcm.length);for(let i=0;i<pcm.length;i++)out[i]=pcm[i]/32768;return out}
function resampleToInt16(input,sourceRate){const ratio=sourceRate/INPUT_RATE,out=new Int16Array(Math.max(1,Math.round(input.length/ratio)));for(let i=0;i<out.length;i++){const pos=i*ratio,left=Math.floor(pos),right=Math.min(left+1,input.length-1),mix=pos-left,value=(input[left]||0)*(1-mix)+(input[right]||0)*mix,clamped=Math.max(-1,Math.min(1,value));out[i]=clamped<0?clamped*32768:clamped*32767}return out}
async function prepareAudio(){const AudioCtx=window.AudioContext||window.webkitAudioContext;if(!AudioCtx||!navigator.mediaDevices?.getUserMedia)throw new Error('voice-not-supported');micContext=new AudioCtx();try{outputContext=new AudioCtx({sampleRate:OUTPUT_RATE})}catch{outputContext=new AudioCtx()}await Promise.all([micContext.resume(),outputContext.resume()]);if(!outputContext.audioWorklet)throw new Error('voice-playback-not-supported');await outputContext.audioWorklet.addModule('./mesraah-voice-playback.worklet.js?v=0.19.0');outputWorklet=new AudioWorkletNode(outputContext,'mesraah-voice-playback');outputGain=outputContext.createGain();outputGain.gain.value=1;outputWorklet.connect(outputGain);outputGain.connect(outputContext.destination);outputQueuedUntil=outputContext.currentTime;micStream=await navigator.mediaDevices.getUserMedia({audio:{channelCount:1,echoCancellation:true,noiseSuppression:true,autoGainControl:true}})}
function clearPlayback(){try{outputWorklet?.port.postMessage({type:'clear'})}catch{}if(outputContext)outputQueuedUntil=outputContext.currentTime}
function suppressMicForModelOutput(){if(!IOS_ECHO_GUARD||micSuppressed)return;micSuppressed=true;if(!streamEndSent&&session){streamEndSent=true;try{session.sendRealtimeInput({audioStreamEnd:true})}catch{}}}
function resumeMicAfterPlayback(){clearTimeout(resumeMicTimer);if(!IOS_ECHO_GUARD){if(active)setStatus('أسمعك الآن','listening');return}const remainingMs=outputContext?Math.max(0,(outputQueuedUntil-outputContext.currentTime)*1000):0;resumeMicTimer=setTimeout(()=>{micSuppressed=false;streamEndSent=false;if(active)setStatus('أسمعك الآن','listening')},remainingMs+140)}
function playPcm(base64){if(!active||!outputContext||!outputWorklet||!base64)return;suppressMicForModelOutput();const samples=base64ToFloat32(base64);if(!samples.length)return;outputQueuedUntil=Math.max(outputContext.currentTime,outputQueuedUntil)+samples.length/OUTPUT_RATE;try{outputWorklet.port.postMessage({samples},[samples.buffer])}catch{outputWorklet.port.postMessage({samples})}setStatus('مسراح يتكلم','speaking')}
async function fetchToken(forceRefresh=false){if(typeof window.MesraahVoiceGetAppCheckToken!=='function')throw new Error('voice-app-check-not-ready');const token=await window.MesraahVoiceGetAppCheckToken({forceRefresh}),response=await fetch(endpoint(),{method:'POST',headers:{'Content-Type':'application/json','X-Firebase-AppCheck':token},body:'{}'}),data=await response.json().catch(()=>({}));if(response.status===401&&!forceRefresh)return fetchToken(true);if(!response.ok||!data.token){const error=new Error(`voice-token-failed:${response.status}`);error.status=response.status;throw error}return data.token}
function withTimeout(promise,ms=TOOL_TIMEOUT_MS){let timer;return Promise.race([Promise.resolve(promise),new Promise((_,reject)=>{timer=setTimeout(()=>reject(new Error('voice-tool-timeout')),ms)})]).finally(()=>clearTimeout(timer))}
async function executeAndPersist(call){let result=await withTimeout(executeTaskTool(call.name,call.args||{}));if(result?.ok&&call.name!=='search_tasks'){try{const cloud=await withTimeout(window.MesraahCloudBridge?.saveNow?.()||Promise.resolve({ok:true,mode:'local'}),8000);if(cloud?.ok===false)return{...result,ok:false,persisted:false,error:'cloud-save-not-confirmed'};result={...result,persisted:true}}catch{return{...result,ok:false,persisted:false,error:'cloud-save-failed'}}}return result}
async function handleToolCalls(calls=[]){const responses=[];setStatus('أنفذ طلبك…','working');for(const call of calls){let result;try{result=await executeAndPersist(call)}catch(error){result={ok:false,error:String(error?.message||error)}}responses.push({name:call.name,id:call.id,response:{result}})}session?.sendToolResponse({functionResponses:responses})}
function handleMessage(message){if(message?.toolCall?.functionCalls?.length)handleToolCalls(message.toolCall.functionCalls).catch(console.error);const content=message?.serverContent;if(!content)return;if(content.interrupted){clearPlayback();micSuppressed=false;streamEndSent=false;setStatus('أسمعك الآن','listening')}if(content.inputTranscription?.text&&!micSuppressed)setDetail(`أنت: ${content.inputTranscription.text}`);if(content.outputTranscription?.text)setDetail(`مسراح: ${content.outputTranscription.text}`);for(const part of content.modelTurn?.parts||[])if(part.inlineData?.data)playPcm(part.inlineData.data);if(content.turnComplete&&active)resumeMicAfterPlayback()}
function startMic(){if(!active||!session||!micContext||!micStream||micProcessor)return;micSource=micContext.createMediaStreamSource(micStream);micProcessor=micContext.createScriptProcessor(2048,1,1);silentGain=micContext.createGain();silentGain.gain.value=0;micProcessor.onaudioprocess=event=>{if(!active||!session||micSuppressed)return;const pcm=resampleToInt16(event.inputBuffer.getChannelData(0),micContext.sampleRate),bytes=new Uint8Array(pcm.buffer,pcm.byteOffset,pcm.byteLength);try{session.sendRealtimeInput({audio:{data:bytesToBase64(bytes),mimeType:`audio/pcm;rate=${INPUT_RATE}`}})}catch{}};micSource.connect(micProcessor);micProcessor.connect(silentGain);silentGain.connect(micContext.destination)}
async function shutdown(){clearTimeout(resumeMicTimer);resumeMicTimer=null;micSuppressed=false;streamEndSent=false;if(micProcessor)micProcessor.onaudioprocess=null;try{micProcessor?.disconnect();micSource?.disconnect();silentGain?.disconnect();outputWorklet?.disconnect();outputGain?.disconnect()}catch{}micStream?.getTracks?.().forEach(track=>track.stop());micStream=null;try{await micContext?.close();await outputContext?.close()}catch{}micContext=outputContext=null;micProcessor=micSource=silentGain=outputWorklet=outputGain=null}
async function start(){if(active)return;ensureUi();injectStyles();document.getElementById('v80VoiceOverlay').hidden=false;active=true;setStatus('أجهز المحادثة…','connecting');setDetail('لحظات وأسمعك.');try{await prepareAudio();const token=await fetchToken();if(!active)return;const ai=new GoogleGenAI({apiKey:token,httpOptions:{apiVersion:'v1alpha'}});session=await ai.live.connect({model:MODEL,config:{responseModalities:[Modality.AUDIO],systemInstruction:contextInstruction(),inputAudioTranscription:{},outputAudioTranscription:{},speechConfig:{voiceConfig:{prebuiltVoiceConfig:{voiceName:'Kore'}}},tools:[{functionDeclarations:TASK_TOOL_DECLARATIONS}]},callbacks:{onopen:()=>setStatus('أتصل بمسراح…','connecting'),onmessage:handleMessage,onerror:event=>console.error('Mesraah Live voice:',event),onclose:()=>{if(active){setStatus('انقطع الاتصال','error');setDetail('انقطع الاتصال الصوتي. حاول مرة أخرى.')}}});if(!active)return;startMic();setStatus('أسمعك الآن','listening');setDetail('تكلم بشكل طبيعي. مسراح يفهم بياناتك الحالية وينفذ معك داخل المنصة.')}catch(error){console.error('Mesraah Live voice start:',error);setStatus('تعذر تشغيل المحادثة الصوتية','error');const status=Number(error?.status)||0;setDetail(status?`تعذر بدء الاتصال الصوتي (${status}).`:'تعذر الاتصال الآن. حاول مرة أخرى بعد قليل.');try{session?.close?.()}catch{}session=null;active=false;await shutdown();throw error}}
async function stop(){active=false;try{session?.close?.()}catch{}session=null;await shutdown();const host=document.getElementById('v80VoiceOverlay');if(host)host.hidden=true}
ensureUi();injectStyles();window.MesraahVoice={start,stop,get active(){return active},mode:'gemini-live-0190'};
