import { GoogleGenAI, Modality } from 'https://cdn.jsdelivr.net/npm/@google/genai@2.14.0/+esm';
import { TASK_TOOL_DECLARATIONS, executeTaskTool } from './mesraah-voice-tools.js?v=0.18.0';

const MODEL='gemini-3.1-flash-live-preview';
const INPUT_RATE=16000;
const OUTPUT_RATE=24000;
const DATA_KEY='mesraah_v030';
let active=false,session=null,micStream=null,micContext=null,outputContext=null,micSource=null,micProcessor=null,silentGain=null,outputGain=null;
let outputQueuedUntil=0,micSuppressed=false,greetingPending=false,greetingTimer=null;
const playingSources=new Set();

function state(){try{return JSON.parse(localStorage.getItem(DATA_KEY)||'{}')||{}}catch{return {}}}
function endpoint(){return String(window.MESRAAH_VOICE_TOKEN_ENDPOINT||'').trim()}
function ui(){return {host:document.getElementById('v80VoiceOverlay'),status:document.getElementById('mesraahVoiceStatus'),detail:document.getElementById('mesraahVoiceDetail')}}
function setStatus(text,mode=''){const x=ui();if(x.status)x.status.textContent=text;if(x.host)x.host.dataset.state=mode}
function setDetail(text){const x=ui();if(x.detail)x.detail.textContent=String(text||'')}
function ensureUi(){if(document.getElementById('v80VoiceOverlay'))return;const host=document.createElement('div');host.id='v80VoiceOverlay';host.className='v80-voice-overlay';host.hidden=true;host.innerHTML='<section class="v80-voice-card" role="dialog" aria-modal="true" aria-label="محادثة صوتية مع مسراح"><div class="v80-voice-top"><div><span class="v80-voice-kicker">مسراح</span><strong>تحدث مع مسراح</strong></div><button type="button" id="mesraahVoiceClose" aria-label="إغلاق">×</button></div><div class="v80-voice-orb"><span></span><span></span><span></span></div><div class="v80-voice-status" id="mesraahVoiceStatus">جاهز</div><div class="mesraah-voice-detail" id="mesraahVoiceDetail">تكلم بشكل طبيعي، ومسراح يسمعك ويتعامل مع مهامك.</div><div class="mesraah-voice-actions"><button type="button" class="v80-voice-stop" id="mesraahVoiceStop">إنهاء المحادثة</button></div></section>';document.body.appendChild(host);document.getElementById('mesraahVoiceClose').onclick=stop;document.getElementById('mesraahVoiceStop').onclick=stop}
function instruction(){const s=state(),tasks=(s.tasks||[]).filter(t=>t.status!=='done').slice(0,40),people=(s.people||[]).slice(0,40),spaces=(s.spaces||[]).slice(0,30);return `أنت مسراح، مساعد شخصي سعودي صوتي بصوت رجل. تكلم باختصار وبلهجة سعودية سهلة. مسراح هو مصدر الحقيقة. استخدم الأدوات للبحث والإضافة والتعديل والحذف والإنجاز. لا تقل تم إلا بعد نجاح الأداة. بيانات مسراح الحالية: ${JSON.stringify({name:s.profile?.name||'',tasks,people,spaces})}`}
function errorMessage(e){const m=String(e?.message||e||'');if(/app.?check/i.test(m))return 'تعذر التحقق من اتصال مسراح. أعد فتح المحادثة.';if(/token|worker|server|503|504/i.test(m))return 'خدمة الصوت لم تستجب الآن. أعد المحاولة.';if(/microphone|permission|notallowed|media/i.test(m))return 'يحتاج مسراح إذن استخدام الميكروفون.';if(/audio|context|worklet/i.test(m))return 'تعذر تشغيل مخرج الصوت في المتصفح.';return `تعذر تشغيل الصوت. ${m.slice(0,90)}`}
function bytesToBase64(bytes){let out='';for(let i=0;i<bytes.length;i+=0x8000)out+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(out)}
function base64ToFloat32(v){const raw=atob(v),n=raw.length-(raw.length%2),buf=new ArrayBuffer(n),u8=new Uint8Array(buf);for(let i=0;i<n;i++)u8[i]=raw.charCodeAt(i);const pcm=new Int16Array(buf),out=new Float32Array(pcm.length);for(let i=0;i<pcm.length;i++)out[i]=pcm[i]/32768;return out}
function resampleToInt16(input,rate){const ratio=rate/INPUT_RATE,out=new Int16Array(Math.max(1,Math.round(input.length/ratio)));for(let i=0;i<out.length;i++){const pos=i*ratio,l=Math.floor(pos),r=Math.min(l+1,input.length-1),mix=pos-l,v=(input[l]||0)*(1-mix)+(input[r]||0)*mix,c=Math.max(-1,Math.min(1,v));out[i]=c<0?c*32768:c*32767}return out}
async function prepareAudio(){const A=window.AudioContext||window.webkitAudioContext;if(!A||!navigator.mediaDevices?.getUserMedia)throw new Error('audio-not-supported');micContext=new A();outputContext=new A();await Promise.all([micContext.resume(),outputContext.resume()]);outputGain=outputContext.createGain();outputGain.gain.value=1;outputGain.connect(outputContext.destination);outputQueuedUntil=outputContext.currentTime;micStream=await navigator.mediaDevices.getUserMedia({audio:{channelCount:1,echoCancellation:true,noiseSuppression:true,autoGainControl:true}})}
function clearPlayback(){for(const src of playingSources){try{src.stop()}catch{}}playingSources.clear();if(outputContext)outputQueuedUntil=outputContext.currentTime}
function playPcm(b64){if(!active||!outputContext||!b64)return;const samples=base64ToFloat32(b64);if(!samples.length)return;micSuppressed=true;const buffer=outputContext.createBuffer(1,samples.length,OUTPUT_RATE);buffer.copyToChannel(samples,0);const src=outputContext.createBufferSource();src.buffer=buffer;src.connect(outputGain);const at=Math.max(outputContext.currentTime+.02,outputQueuedUntil);src.start(at);outputQueuedUntil=at+buffer.duration;playingSources.add(src);src.onended=()=>playingSources.delete(src);setStatus('مسراح يتكلم','speaking')}
async function appCheckToken(force=false){if(typeof window.MesraahVoiceGetAppCheckToken!=='function')throw new Error('app-check-not-ready');try{return await window.MesraahVoiceGetAppCheckToken({forceRefresh:force})}catch(e){if(!force)return window.MesraahVoiceGetAppCheckToken({forceRefresh:true});throw e}}
async function fetchToken(){const url=endpoint();if(!url)throw new Error('voice-token-endpoint-missing');let last;for(let i=0;i<2;i++){try{const ac=new AbortController(),timer=setTimeout(()=>ac.abort(),9000);const app=await appCheckToken(i>0);const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json','X-Firebase-AppCheck':app},body:'{}',signal:ac.signal});clearTimeout(timer);const d=await r.json().catch(()=>({}));if(r.ok&&d.token)return d.token;last=new Error(`voice-token-${r.status}`)}catch(e){last=e}await new Promise(r=>setTimeout(r,500))}throw last||new Error('voice-token-failed')}
async function handleTools(calls=[]){if(!session||!calls.length)return;setStatus('أنفذ طلبك…','connecting');const responses=[];for(const call of calls){let result;try{result=await executeTaskTool(call.name,call.args||{});if(result?.ok&&call.name!=='search_tasks')await window.MesraahCloudBridge?.saveNow?.()}catch(e){result={ok:false,error:String(e?.message||e)}}responses.push({name:call.name,id:call.id,response:{result}})}try{session.sendToolResponse({functionResponses:responses})}catch(e){console.error('Mesraah voice tool response',e)}}
function handleMessage(m){if(m?.toolCall?.functionCalls?.length)handleTools(m.toolCall.functionCalls);const c=m?.serverContent;if(!c)return;if(c.interrupted){clearPlayback();micSuppressed=false;setStatus('أسمعك الآن','listening')}if(c.inputTranscription?.text)setDetail(`أنت: ${c.inputTranscription.text}`);if(c.outputTranscription?.text)setDetail(`مسراح: ${c.outputTranscription.text}`);let audio=false;for(const p of c.modelTurn?.parts||[]){if(p.inlineData?.data){audio=true;playPcm(p.inlineData.data)}}if(audio&&greetingPending){greetingPending=false;clearTimeout(greetingTimer)}if(c.turnComplete&&active){const delay=Math.max(0,(outputQueuedUntil-(outputContext?.currentTime||0))*1000)+120;setTimeout(()=>{if(!active)return;micSuppressed=false;setStatus('أسمعك الآن','listening')},delay)}}
function startMic(){if(!active||!session||!micContext||!micStream||micProcessor)return;micSource=micContext.createMediaStreamSource(micStream);micProcessor=micContext.createScriptProcessor(2048,1,1);silentGain=micContext.createGain();silentGain.gain.value=0;micProcessor.onaudioprocess=e=>{if(!active||!session||micSuppressed)return;const pcm=resampleToInt16(e.inputBuffer.getChannelData(0),micContext.sampleRate),bytes=new Uint8Array(pcm.buffer,pcm.byteOffset,pcm.byteLength);try{session.sendRealtimeInput({audio:{data:bytesToBase64(bytes),mimeType:'audio/pcm;rate=16000'}})}catch(err){console.error('Mesraah voice input',err)}};micSource.connect(micProcessor);micProcessor.connect(silentGain);silentGain.connect(micContext.destination)}
async function shutdown(){clearTimeout(greetingTimer);clearPlayback();if(micProcessor)micProcessor.onaudioprocess=null;try{micProcessor?.disconnect();micSource?.disconnect();silentGain?.disconnect();outputGain?.disconnect()}catch{}micProcessor=micSource=silentGain=outputGain=null;micStream?.getTracks?.().forEach(t=>t.stop());micStream=null;try{await micContext?.close();await outputContext?.close()}catch{}micContext=outputContext=null}
async function start(){if(active)return;ensureUi();const host=document.getElementById('v80VoiceOverlay');host.hidden=false;active=true;setStatus('أجهز المحادثة…','connecting');setDetail('أتحقق من الميكروفون والصوت والاتصال.');try{await prepareAudio();setDetail('أتصل بمسراح…');const token=await fetchToken();const ai=new GoogleGenAI({apiKey:token,httpOptions:{apiVersion:'v1alpha'}});session=await ai.live.connect({model:MODEL,config:{responseModalities:[Modality.AUDIO],systemInstruction:instruction(),inputAudioTranscription:{},outputAudioTranscription:{},speechConfig:{voiceConfig:{prebuiltVoiceConfig:{voiceName:'Orus'}}},tools:[{functionDeclarations:TASK_TOOL_DECLARATIONS}]},callbacks:{onopen:()=>setDetail('تم الاتصال. أختبر الصوت الآن…'),onmessage:handleMessage,onerror:e=>{console.error('Mesraah voice error',e);if(active){setStatus('مشكلة في الاتصال','error');setDetail(errorMessage(e))}},onclose:e=>{if(active){setStatus('انقطع الاتصال','error');setDetail(`انقطع اتصال الصوت${e?.reason?`: ${e.reason}`:''}`)}}});if(!active)return;startMic();micSuppressed=true;greetingPending=true;setStatus('أختبر الصوت…','connecting');session.sendRealtimeInput({text:'ابدأ بتحية صوتية قصيرة جدا: هلا، أنا مسراح. ثم توقف وانتظر المستخدم.'});greetingTimer=setTimeout(()=>{if(active&&greetingPending){setStatus('اتصلت الجلسة لكن لم يصل صوت','error');setDetail('Gemini اتصل، لكن لم يرسل مسار صوت. أعد فتح المحادثة مرة واحدة.')}},9000)}catch(e){console.error('Mesraah voice start',e);setStatus('تعذر تشغيل المحادثة الصوتية','error');setDetail(errorMessage(e));await shutdown();active=false}}
async function stop(){active=false;try{session?.close?.()}catch{}session=null;await shutdown();const host=document.getElementById('v80VoiceOverlay');if(host)setTimeout(()=>host.hidden=true,100)}
window.MesraahVoice={start,stop,get active(){return active}};
ensureUi();
